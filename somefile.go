//this package is considered to be the module name since it is at the top level of the module
// named after the last part of the module path so this code is exported
package gomodlib

func Foo() string {
	return "gomodlib Foo"
}
